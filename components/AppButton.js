import {
  View,
  Text,
  StyleSheet,
  Button
} from 'react-native'
import React from 'react'

const AppButton = ({ title }) => <Button title={title} />

export default AppButton

const styles = StyleSheet.create({})
